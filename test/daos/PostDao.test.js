// const PostDao = jest.mock("./../../daos/PostDao.ts");
const sinon = require("sinon");
// const {Post} = PostDao;

test('getPost_happyCase', () => {
    console.log(sinon);
    // console.log(PostDao);
    // console.log(Post);
    return Promise.resolve(true);
});